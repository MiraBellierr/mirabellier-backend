const {
  getCurrentRecordedDate,
  normalizeArenaEffects,
  rarityFromCharacterRank,
  xpToNext,
} = require("./legacy-service");

module.exports = {
  getCurrentRecordedDate,
  normalizeArenaEffects,
  rarityFromCharacterRank,
  xpToNext,
};
