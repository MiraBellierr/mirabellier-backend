# Keeps dependency advisories from silently re-accumulating: Dependabot opens
# grouped weekly PRs for the backend's npm dependencies.
version: 2
updates:
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    groups:
      backend-minor-patch:
        update-types:
          - minor
          - patch
